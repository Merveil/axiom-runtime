[build-system]
requires = ["maturin>=1.4,<2"]
build-backend = "maturin"

[project]
name = "axiom-core"
version = "0.1.0"
description = "Rust acceleration layer for axiom-runtime"
requires-python = ">=3.10"

[tool.maturin]
module-name = "axiom_core"
features = ["pyo3/extension-module"]
